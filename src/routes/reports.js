const router=require('express').Router();const supabase=require('../supabase');const{requireAuth}=require('../middleware/auth');
router.get('/',requireAuth,async(req,res)=>{try{const{data}=await supabase.from('reports').select('*,connections(account_name,platform,currency),workspaces(name)').eq('created_by',req.user.id).order('created_at',{ascending:false});res.json({reports:data||[]})}catch(e){res.status(500).json({error:e.message})}});
router.post('/',requireAuth,async(req,res)=>{try{const{workspace_id,connection_id,name,campaign_ids,metrics,date_preset,allow_date_change}=req.body;const{data:ws}=await supabase.from('workspaces').select('id').eq('id',workspace_id).eq('owner_id',req.user.id).single();if(!vs)return res.status(403).json({error:'Not your workspace'});const{data,error}=await supabase.from('reports').insert({workspace_id,connection_id,name,campaign_ids:campaign_ids||[],metrics:metrics||['spend','impressions','clicks','ctr','cpm','results','cpr'],date_preset:date_preset||'last_30d',allow_date_change:allow_date_change||false,created_by:req.user.id}).select().single();if(error)return res.status(500).json({error:error.message});res.json({report:data})}catch(e){res.status(500).json({error:e.message})}});
router.post('/:id/invite',requireAuth,async(req,res)=>{try{const{email}=req.body;if(!email)return res.status(400).json({error:'Email required'});const{data:report}=await supabase.from('reports').select('id').eq('id',req.params.id).eq('created_by',req.user.id).single();if(!report)return res.status(403).json({error:'Not your report'});const{data:existingUser}=await supabase.from('users').select('id').eq('email',email).single();const{error}=await supabase.from('report_access').upsert({report_id:req.params.id,user_id:existingUser?.id||null,email},{onConflict:'report_id,email'});if(error)return res.status(500).json({error:error.message});res.json({success:true})}catch(e){res.status(500).json({error:e.message})}});
router.get('/public/:token',async(req,res)=>>{try{const{data:report}=await supabase.from('reports').select('*,connections(account_name,platform,currency)').eq('public_token',req.params.token).eq('is_public',true).single();if(!report)return res.status(404).json({error:'Not found'});res.json({report})}catch(e){res.status(500).json({error:e.message})}});
module.exports=router;
